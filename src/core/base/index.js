/**
 * Central export for all base classes
 */

const BaseController = require('./BaseController');
const BaseService = require('./BaseService');
const BaseRepository = require('./BaseRepository');

module.exports = {
  BaseController,
  BaseService,
  BaseRepository
};
