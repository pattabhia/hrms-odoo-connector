/**
 * Central export for all interfaces
 */

const IRepository = require('./IRepository');
const IService = require('./IService');
const IOdooClient = require('./IOdooClient');

module.exports = {
  IRepository,
  IService,
  IOdooClient
};
